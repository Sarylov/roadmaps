---
title: dependencies
summary: dependencies в блоке «Clean Architecture» — нужно уметь объяснить механизм, риск и альтернативы.
---

## Зачем нужно

База уровня CORE. Границы слоёв и направление зависимостей в приложении. Границы модулей и направление зависимостей.

## Как работает

**dependencies** — тема блока «Clean Architecture» (architecture). Границы слоёв и направление зависимостей в приложении.

Типичная ошибка — использовать dependencies «по привычке» без понимания границ и failure modes в «Clean Architecture».

Границы модулей и направление зависимостей.

## Что спрашивают

- Объясните dependencies своими словами на примере из «Clean Architecture».
- Какие ошибки и edge cases связаны с dependencies?
- Какие альтернативы dependencies и когда они лучше?

## Ответы

### Объясните dependencies своими словами на примере из «Clean Architecture».

**dependencies** — тема блока «Clean Architecture» (architecture). Границы слоёв и направление зависимостей в приложении. Держите структуру: проблема → механизм → пример. Границы модулей и направление зависимостей.

### Какие ошибки и edge cases связаны с dependencies?

Типичная ошибка — использовать dependencies «по привычке» без понимания границ и failure modes в «Clean Architecture». Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы dependencies и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Границы модулей и направление зависимостей.
