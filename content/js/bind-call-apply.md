---
title: bind/call/apply
summary: "bind/call/apply: call/apply вызывают с this и аргументами; bind возвращает обёртку. Важно на собесе и в проде в контексте «Функции и контекст»."
---

## Зачем нужно

База уровня CORE. Язык, runtime и асинхронность, на которых держится весь фронт. Упор на event loop, this, coercion и практические ловушки runtime.

## Как работает

**bind/call/apply**: call/apply вызывают с this и аргументами; bind возвращает обёртку.

bind не «перебиндить» повторно уже bound функцию.

apply удобен для массива аргументов; rest/spread чаще современнее.

MDN: [JavaScript Guide](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide).

## Что спрашивают

- Объясните bind/call/apply своими словами на примере из «Функции и контекст».
- Какие ошибки и edge cases связаны с bind/call/apply?
- Какие альтернативы bind/call/apply и когда они лучше?

## Ответы

### Объясните bind/call/apply своими словами на примере из «Функции и контекст».

call/apply вызывают с this и аргументами; bind возвращает обёртку. Держите структуру: проблема → механизм → пример. apply удобен для массива аргументов; rest/spread чаще современнее.

### Какие ошибки и edge cases связаны с bind/call/apply?

bind не «перебиндить» повторно уже bound функцию. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы bind/call/apply и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. apply удобен для массива аргументов; rest/spread чаще современнее.
