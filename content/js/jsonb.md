---
title: JSONB
summary: "JSONB: JSONB — бинарный JSON с индексами GIN. Важно на собесе и в проде в контексте «PostgreSQL»."
---

## Зачем нужно

База уровня CORE. Основная relational database в Node.js backend stack. Упор на event loop, this, coercion и практические ловушки runtime.

## Как работает

**JSONB**: JSONB — бинарный JSON с индексами GIN.

Удобно для гибких атрибутов; легко превратить в свалку.

Частые поля лучше колонок.

MDN: [JavaScript Guide](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide).

## Что спрашивают

- Объясните JSONB своими словами на примере из «PostgreSQL».
- Какие ошибки и edge cases связаны с JSONB?
- Какие альтернативы JSONB и когда они лучше?

## Ответы

### Объясните JSONB своими словами на примере из «PostgreSQL».

JSONB — бинарный JSON с индексами GIN. Держите структуру: проблема → механизм → пример. Частые поля лучше колонок.

### Какие ошибки и edge cases связаны с JSONB?

Удобно для гибких атрибутов; легко превратить в свалку. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы JSONB и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Частые поля лучше колонок.
