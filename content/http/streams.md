---
title: streams
summary: "streams: HTTP/2 streams — независимые потоки кадров. Важно на собесе и в проде в контексте «HTTP/2 / HTTP/3»."
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Современные версии HTTP и их особенности. Упор на семантику метода/статуса/заголовков и кэш.

## Как работает

**streams**: HTTP/2 streams — независимые потоки кадров.

Приоритеты и flow control.

Не путать с Node Stream API.

MDN: [HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP).

## Что спрашивают

- Объясните streams своими словами на примере из «HTTP/2 / HTTP/3».
- Какие ошибки и edge cases связаны с streams?
- Какие альтернативы streams и когда они лучше?

## Ответы

### Объясните streams своими словами на примере из «HTTP/2 / HTTP/3».

HTTP/2 streams — независимые потоки кадров. Держите структуру: проблема → механизм → пример. Не путать с Node Stream API.

### Какие ошибки и edge cases связаны с streams?

Приоритеты и flow control. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы streams и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Не путать с Node Stream API.
