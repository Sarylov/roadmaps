---
title: auth
summary: "auth: Auth на gateway: terminate TLS, проверить токен, прокинуть identity. Важно на собесе и в проде в контексте «API Gateway / BFF»."
---

## Зачем нужно

База уровня CORE. Единая точка входа для клиентов и агрегация вызовов. Упор на семантику метода/статуса/заголовков и кэш.

## Как работает

**auth**: Auth на gateway: terminate TLS, проверить токен, прокинуть identity.

Gateway не единственная authz точка.

Сервис всё равно проверяет права на свои данные.

MDN: [HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP).

## Что спрашивают

- Объясните auth своими словами на примере из «API Gateway / BFF».
- Какие ошибки и edge cases связаны с auth?
- Какие альтернативы auth и когда они лучше?

## Ответы

### Объясните auth своими словами на примере из «API Gateway / BFF».

Auth на gateway: terminate TLS, проверить токен, прокинуть identity. Держите структуру: проблема → механизм → пример. Сервис всё равно проверяет права на свои данные.

### Какие ошибки и edge cases связаны с auth?

Gateway не единственная authz точка. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы auth и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Сервис всё равно проверяет права на свои данные.
