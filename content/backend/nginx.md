---
title: Nginx
summary: Nginx в блоке «Reverse Proxy» — нужно уметь объяснить механизм, риск и альтернативы.
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Промежуточный слой перед backend-приложением. Слои приложения, DI и границы транзакций.

## Как работает

**Nginx** — тема блока «Reverse Proxy» (backend). Промежуточный слой перед backend-приложением.

Типичная ошибка — использовать Nginx «по привычке» без понимания границ и failure modes в «Reverse Proxy».

Слои приложения, DI и границы транзакций.

## Что спрашивают

- Что такое Nginx и какую задачу закрывает?
- Какие ключевые абстракции Nginx нужно знать на собесе?
- Какие operational pitfalls у Nginx?

## Ответы

### Что такое Nginx и какую задачу закрывает?

**Nginx** — тема блока «Reverse Proxy» (backend). Промежуточный слой перед backend-приложением. Опишите место в стеке «Reverse Proxy». Слои приложения, DI и границы транзакций.

### Какие ключевые абстракции Nginx нужно знать на собесе?

Назовите 3–5 сущностей/операций и как они стыкуются. Типичная ошибка — использовать Nginx «по привычке» без понимания границ и failure modes в «Reverse Proxy».

### Какие operational pitfalls у Nginx?

Типичная ошибка — использовать Nginx «по привычке» без понимания границ и failure modes в «Reverse Proxy». Говорите про деплой, мониторинг, лимиты и failure modes, не только про happy path.
