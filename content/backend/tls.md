---
title: TLS
summary: TLS в блоке «Reverse Proxy» — нужно уметь объяснить механизм, риск и альтернативы.
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Промежуточный слой перед backend-приложением. Слои приложения, DI и границы транзакций.

## Как работает

**TLS** — тема блока «Reverse Proxy» (backend). Промежуточный слой перед backend-приложением.

Типичная ошибка — использовать TLS «по привычке» без понимания границ и failure modes в «Reverse Proxy».

Слои приложения, DI и границы транзакций.

## Что спрашивают

- Объясните TLS своими словами на примере из «Reverse Proxy».
- Какие ошибки и edge cases связаны с TLS?
- Какие альтернативы TLS и когда они лучше?

## Ответы

### Объясните TLS своими словами на примере из «Reverse Proxy».

**TLS** — тема блока «Reverse Proxy» (backend). Промежуточный слой перед backend-приложением. Держите структуру: проблема → механизм → пример. Слои приложения, DI и границы транзакций.

### Какие ошибки и edge cases связаны с TLS?

Типичная ошибка — использовать TLS «по привычке» без понимания границ и failure modes в «Reverse Proxy». Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы TLS и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Слои приложения, DI и границы транзакций.
