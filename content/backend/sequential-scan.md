---
title: sequential scan
summary: "sequential scan: Seq scan читает всю таблицу. Важно на собесе и в проде в контексте «Query Optimization»."
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Поиск узких мест и анализ планов выполнения. Слои приложения, DI и границы транзакций.

## Как работает

**sequential scan**: Seq scan читает всю таблицу.

Нормален на маленьких/неселективных.

Проблема на больших таблицах с селективным WHERE без индекса.

## Что спрашивают

- Объясните sequential scan своими словами на примере из «Query Optimization».
- Какие ошибки и edge cases связаны с sequential scan?
- Какие альтернативы sequential scan и когда они лучше?

## Ответы

### Объясните sequential scan своими словами на примере из «Query Optimization».

Seq scan читает всю таблицу. Держите структуру: проблема → механизм → пример. Проблема на больших таблицах с селективным WHERE без индекса.

### Какие ошибки и edge cases связаны с sequential scan?

Нормален на маленьких/неселективных. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы sequential scan и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Проблема на больших таблицах с селективным WHERE без индекса.
