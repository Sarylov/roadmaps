---
title: SELECT
summary: "SELECT: SELECT проекция столбцов; избегайте SELECT * в проде. Важно на собесе и в проде в контексте «SQL»."
---

## Зачем нужно

Частый KILLER-вопрос на собеседованиях. Основной язык работы с реляционными данными. Где уместно — Postgres, планы EXPLAIN и индексы.

## Как работает

**SELECT**: SELECT проекция столбцов; избегайте SELECT * в проде.

Покрывающие индексы под SELECT list.

Только нужные поля снижают IO.

Документация: [PostgreSQL](https://www.postgresql.org/docs/current/).

## Что спрашивают

- Как работает SELECT и какая у него семантика?
- Чем SELECT отличается от близких API в «SQL»?
- Какой edge case с SELECT чаще всего ловят на собесе?

## Ответы

### Как работает SELECT и какая у него семантика?

SELECT проекция столбцов; избегайте SELECT * в проде. Умейте показать крошечный пример и объяснить edge case. Только нужные поля снижают IO.

### Чем SELECT отличается от близких API в «SQL»?

Сравните контракт: успех/ошибка, идемпотентность, сложность, стоимость. Покрывающие индексы под SELECT list.

### Какой edge case с SELECT чаще всего ловят на собесе?

Покрывающие индексы под SELECT list. Добавьте, как тестировать и что будет в production под нагрузкой.
