---
title: GROUP BY
summary: "GROUP BY: GROUP BY агрегирует; SELECT только группируемые/агрегаты. Важно на собесе и в проде в контексте «SQL»."
---

## Зачем нужно

Частый KILLER-вопрос на собеседованиях. Основной язык работы с реляционными данными. Где уместно — Postgres, планы EXPLAIN и индексы.

## Как работает

**GROUP BY**: GROUP BY агрегирует; SELECT только группируемые/агрегаты.

В PG строгость зависит от настроек.

Частая ошибка — «лишние» колонки.

Документация: [PostgreSQL](https://www.postgresql.org/docs/current/).

## Что спрашивают

- Как работает GROUP BY и какая у него семантика?
- Чем GROUP BY отличается от близких API в «SQL»?
- Какой edge case с GROUP BY чаще всего ловят на собесе?

## Ответы

### Как работает GROUP BY и какая у него семантика?

GROUP BY агрегирует; SELECT только группируемые/агрегаты. Умейте показать крошечный пример и объяснить edge case. Частая ошибка — «лишние» колонки.

### Чем GROUP BY отличается от близких API в «SQL»?

Сравните контракт: успех/ошибка, идемпотентность, сложность, стоимость. В PG строгость зависит от настроек.

### Какой edge case с GROUP BY чаще всего ловят на собесе?

В PG строгость зависит от настроек. Добавьте, как тестировать и что будет в production под нагрузкой.
