---
title: WHERE
summary: "WHERE: WHERE фильтрует строки до группировки. Важно на собесе и в проде в контексте «SQL»."
---

## Зачем нужно

Частый KILLER-вопрос на собеседованиях. Основной язык работы с реляционными данными. Где уместно — Postgres, планы EXPLAIN и индексы.

## Как работает

**WHERE**: WHERE фильтрует строки до группировки.

SARGable предикаты (не wrapping колонки функцией).

Порядок условий не заменяет индексы.

Документация: [PostgreSQL](https://www.postgresql.org/docs/current/).

## Что спрашивают

- Как работает WHERE и какая у него семантика?
- Чем WHERE отличается от близких API в «SQL»?
- Какой edge case с WHERE чаще всего ловят на собесе?

## Ответы

### Как работает WHERE и какая у него семантика?

WHERE фильтрует строки до группировки. Умейте показать крошечный пример и объяснить edge case. Порядок условий не заменяет индексы.

### Чем WHERE отличается от близких API в «SQL»?

Сравните контракт: успех/ошибка, идемпотентность, сложность, стоимость. SARGable предикаты (не wrapping колонки функцией).

### Какой edge case с WHERE чаще всего ловят на собесе?

SARGable предикаты (не wrapping колонки функцией). Добавьте, как тестировать и что будет в production под нагрузкой.
