---
title: branches
summary: "branches — это ветвление: trunk-based vs long-lived feature."
---

## Зачем нужно

Чтобы явно выразить и переиспользовать поведение, связанное с «branches», а не держать его зашитым в одном месте.

## Как работает

branches — это ветвление: trunk-based vs long-lived feature.

Частая ошибка: долгие ветки = ад merge.

## Что спрашивают

- Что такое branches простыми словами?
- Зачем в коде нужен branches?
- Какие ошибки и ограничения связаны с branches?

## Ответы

### Что такое branches простыми словами?

branches — это ветвление: trunk-based vs long-lived feature.

### Зачем в коде нужен branches?

Чтобы явно выразить и переиспользовать поведение, связанное с «branches», а не держать его зашитым в одном месте.

### Какие ошибки и ограничения связаны с branches?

Долгие ветки = ад merge. Имеет смысл сравнить с ближайшей альтернативой и понять, когда механизм избыточен.
