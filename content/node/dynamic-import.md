---
title: dynamic import
summary: "dynamic import: import() — ленивая загрузка модуля → Promise. Важно на собесе и в проде в контексте «Модули»."
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Язык, runtime и асинхронность, на которых держится весь фронт. Событийный цикл Node, backpressure и блокировки потока.

## Как работает

**dynamic import**: import() — ленивая загрузка модуля → Promise.

Нужен для code splitting маршрутов.

Ошибки сети/чанка надо обрабатывать UI fallback.

Документация: [Node.js](https://nodejs.org/docs/latest/api/).

## Что спрашивают

- Объясните dynamic import своими словами на примере из «Модули».
- Какие ошибки и edge cases связаны с dynamic import?
- Какие альтернативы dynamic import и когда они лучше?

## Ответы

### Объясните dynamic import своими словами на примере из «Модули».

import() — ленивая загрузка модуля → Promise. Держите структуру: проблема → механизм → пример. Ошибки сети/чанка надо обрабатывать UI fallback.

### Какие ошибки и edge cases связаны с dynamic import?

Нужен для code splitting маршрутов. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы dynamic import и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Ошибки сети/чанка надо обрабатывать UI fallback.
