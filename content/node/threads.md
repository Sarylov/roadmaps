---
title: threads
summary: "threads: Web Worker — фоновый поток без DOM. Важно на собесе и в проде в контексте «Web Workers»."
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Как страница парсится, рисуется, работает и живёт в фоне. Событийный цикл Node, backpressure и блокировки потока.

## Как работает

**threads**: Web Worker — фоновый поток без DOM.

Обмен postMessage/structured clone (или transferable).

Не для маленьких задач — накладные расходы.

Документация: [Node.js](https://nodejs.org/docs/latest/api/).

## Что спрашивают

- Объясните threads своими словами на примере из «Web Workers».
- Какие ошибки и edge cases связаны с threads?
- Какие альтернативы threads и когда они лучше?

## Ответы

### Объясните threads своими словами на примере из «Web Workers».

Web Worker — фоновый поток без DOM. Держите структуру: проблема → механизм → пример. Не для маленьких задач — накладные расходы.

### Какие ошибки и edge cases связаны с threads?

Обмен postMessage/structured clone (или transferable). Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы threads и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Не для маленьких задач — накладные расходы.
