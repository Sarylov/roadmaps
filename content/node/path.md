---
title: path
summary: "path: path.join/pathURL учитывают платформу. Важно на собесе и в проде в контексте «Node.js APIs»."
---

## Зачем нужно

База уровня CORE. Базовые модули runtime, необходимые для backend-разработки. Событийный цикл Node, backpressure и блокировки потока.

## Как работает

**path**: path.join/pathURL учитывают платформу.

Не клеить пути строками с слэшами вслепую.

path.normalize не заменяет security checks traversal.

Документация: [Node.js](https://nodejs.org/docs/latest/api/).

## Что спрашивают

- Как работает path и какая у него семантика?
- Чем path отличается от близких API в «Node.js APIs»?
- Какой edge case с path чаще всего ловят на собесе?

## Ответы

### Как работает path и какая у него семантика?

path.join/pathURL учитывают платформу. Умейте показать крошечный пример и объяснить edge case. path.normalize не заменяет security checks traversal.

### Чем path отличается от близких API в «Node.js APIs»?

Сравните контракт: успех/ошибка, идемпотентность, сложность, стоимость. Не клеить пути строками с слэшами вслепую.

### Какой edge case с path чаще всего ловят на собесе?

Не клеить пути строками с слэшами вслепую. Добавьте, как тестировать и что будет в production под нагрузкой.
