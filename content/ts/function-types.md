---
title: function types
summary: "function types: Типы функций: параметры контравариантны (strict), return ковариантен. Важно на собесе и в проде в контексте «Функции и объекты»."
---

## Зачем нужно

База уровня CORE. Типизация функций, объектов и reusable-компонентов. Упор на систему типов, inference и дизайн публичного API.

## Как работает

**function types**: Типы функций: параметры контравариантны (strict), return ковариантен.

callable + construct signatures.

Предпочитайте явные typedef для колбэков публичного API.

Документация: [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html).

## Что спрашивают

- Объясните function types своими словами на примере из «Функции и объекты».
- Какие ошибки и edge cases связаны с function types?
- Какие альтернативы function types и когда они лучше?

## Ответы

### Объясните function types своими словами на примере из «Функции и объекты».

Типы функций: параметры контравариантны (strict), return ковариантен. Держите структуру: проблема → механизм → пример. Предпочитайте явные typedef для колбэков публичного API.

### Какие ошибки и edge cases связаны с function types?

callable + construct signatures. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы function types и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Предпочитайте явные typedef для колбэков публичного API.
