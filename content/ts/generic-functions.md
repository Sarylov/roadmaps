---
title: generic functions
summary: "generic functions: <T> на функции связывает вход/выход. Важно на собесе и в проде в контексте «Функции и объекты»."
---

## Зачем нужно

База уровня CORE. Типизация функций, объектов и reusable-компонентов. Упор на систему типов, inference и дизайн публичного API.

## Как работает

**generic functions**: <T> на функции связывает вход/выход.

Infer из аргументов — DX.

Лишние параметры типов усложняют вызов.

Документация: [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html).

## Что спрашивают

- Объясните generic functions своими словами на примере из «Функции и объекты».
- Какие ошибки и edge cases связаны с generic functions?
- Какие альтернативы generic functions и когда они лучше?

## Ответы

### Объясните generic functions своими словами на примере из «Функции и объекты».

<T> на функции связывает вход/выход. Держите структуру: проблема → механизм → пример. Лишние параметры типов усложняют вызов.

### Какие ошибки и edge cases связаны с generic functions?

Infer из аргументов — DX. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы generic functions и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Лишние параметры типов усложняют вызов.
