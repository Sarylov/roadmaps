---
title: lockfile
summary: "lockfile: Lockfile фиксирует дерево зависимостей. Важно на собесе и в проде в контексте «npm / yarn / pnpm»."
---

## Зачем нужно

База уровня CORE. Сборка, пакеты, качество кода и delivery pipeline. DX и ловля ошибок до прода.

## Как работает

**lockfile**: Lockfile фиксирует дерево зависимостей.

Коммитить lockfile в apps — must.

Расхождения CI vs local из-за игнора lockfile.

## Что спрашивают

- Объясните lockfile своими словами на примере из «npm / yarn / pnpm».
- Какие ошибки и edge cases связаны с lockfile?
- Какие альтернативы lockfile и когда они лучше?

## Ответы

### Объясните lockfile своими словами на примере из «npm / yarn / pnpm».

Lockfile фиксирует дерево зависимостей. Держите структуру: проблема → механизм → пример. Расхождения CI vs local из-за игнора lockfile.

### Какие ошибки и edge cases связаны с lockfile?

Коммитить lockfile в apps — must. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы lockfile и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Расхождения CI vs local из-за игнора lockfile.
