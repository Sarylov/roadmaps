---
title: queries
summary: "queries — это rTL queries: get/find/query + роли/текст."
---

## Зачем нужно

Чтобы явно выразить и переиспользовать поведение, связанное с «queries», а не держать его зашитым в одном месте.

## Как работает

queries — это rTL queries: get/find/query + роли/текст.

Частая ошибка: предпочитайте role/label, не test-id без нужды.

## Что спрашивают

- Что такое queries простыми словами?
- Зачем в коде нужен queries?
- Какие ошибки и ограничения связаны с queries?

## Ответы

### Что такое queries простыми словами?

queries — это rTL queries: get/find/query + роли/текст.

### Зачем в коде нужен queries?

Чтобы явно выразить и переиспользовать поведение, связанное с «queries», а не держать его зашитым в одном месте.

### Какие ошибки и ограничения связаны с queries?

Предпочитайте role/label, не test-id без нужды. Имеет смысл сравнить с ближайшей альтернативой и понять, когда механизм избыточен.
