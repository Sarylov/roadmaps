---
title: map/reduce
summary: "map/reduce: map/filter/reduce — ядро трансформаций массивов. Важно на собесе и в проде в контексте «Массивы и коллекции»."
---

## Зачем нужно

База уровня CORE. Язык, runtime и асинхронность, на которых держится весь фронт. UX, bundle и архитектурные границы UI.

## Как работает

**map/reduce**: map/filter/reduce — ядро трансформаций массивов.

reduce злоупотребляют до нечитаемости; мутация в map — баг.

Для простых сумм reduce ок; сложная логика — явный цикл/хелпер.

## Что спрашивают

- Объясните map/reduce своими словами на примере из «Массивы и коллекции».
- Какие ошибки и edge cases связаны с map/reduce?
- Какие альтернативы map/reduce и когда они лучше?

## Ответы

### Объясните map/reduce своими словами на примере из «Массивы и коллекции».

map/filter/reduce — ядро трансформаций массивов. Держите структуру: проблема → механизм → пример. Для простых сумм reduce ок; сложная логика — явный цикл/хелпер.

### Какие ошибки и edge cases связаны с map/reduce?

reduce злоупотребляют до нечитаемости; мутация в map — баг. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы map/reduce и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Для простых сумм reduce ок; сложная логика — явный цикл/хелпер.
