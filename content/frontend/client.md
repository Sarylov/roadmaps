---
title: client
summary: "client: API client инкапсулирует HTTP, ошибки, auth headers. Важно на собесе и в проде в контексте «API слой»."
---

## Зачем нужно

Частый KILLER-вопрос на собеседованиях. Продуктовые требования, API и компонентная декомпозиция. UX, bundle и архитектурные границы UI.

## Как работает

**client**: API client инкапсулирует HTTP, ошибки, auth headers.

Размазанный fetch по компонентам — боль.

Централизуйте retry/error mapping.

## Что спрашивают

- Объясните client своими словами на примере из «API слой».
- Какие ошибки и edge cases связаны с client?
- Какие альтернативы client и когда они лучше?

## Ответы

### Объясните client своими словами на примере из «API слой».

API client инкапсулирует HTTP, ошибки, auth headers. Держите структуру: проблема → механизм → пример. Централизуйте retry/error mapping.

### Какие ошибки и edge cases связаны с client?

Размазанный fetch по компонентам — боль. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы client и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Централизуйте retry/error mapping.
