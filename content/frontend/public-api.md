---
title: public API
summary: "public API: Public API модуля — единственная точка импорта. Важно на собесе и в проде в контексте «Feature-Sliced Design»."
---

## Зачем нужно

Частый KILLER-вопрос на собеседованиях. Границы модулей, зависимости и поддерживаемость React-кода. UX, bundle и архитектурные границы UI.

## Как работает

**public API**: Public API модуля — единственная точка импорта.

Deep import ломает инкапсуляцию.

ESLint boundaries помогают.

## Что спрашивают

- Объясните public API своими словами на примере из «Feature-Sliced Design».
- Какие ошибки и edge cases связаны с public API?
- Какие альтернативы public API и когда они лучше?

## Ответы

### Объясните public API своими словами на примере из «Feature-Sliced Design».

Public API модуля — единственная точка импорта. Держите структуру: проблема → механизм → пример. ESLint boundaries помогают.

### Какие ошибки и edge cases связаны с public API?

Deep import ломает инкапсуляцию. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы public API и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. ESLint boundaries помогают.
