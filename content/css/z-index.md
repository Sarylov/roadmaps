---
title: z-index
summary: "z-index: z-index сравнивается только внутри одного stacking context. Важно на собесе и в проде в контексте «Position/z-index»."
---

## Зачем нужно

OPT-тема: отличает глубину кандидата. Каркас интерфейса: семантика, раскладка, формы и доступность. Упор на каскад, layout и доступность.

## Как работает

**z-index**: z-index сравнивается только внутри одного stacking context.

Огромные z-index — запах неправильной модели слоёв.

Сначала структура context, потом локальные z-index.

MDN: [CSS](https://developer.mozilla.org/en-US/docs/Web/CSS).

## Что спрашивают

- Объясните z-index своими словами на примере из «Position/z-index».
- Какие ошибки и edge cases связаны с z-index?
- Какие альтернативы z-index и когда они лучше?

## Ответы

### Объясните z-index своими словами на примере из «Position/z-index».

z-index сравнивается только внутри одного stacking context. Держите структуру: проблема → механизм → пример. Сначала структура context, потом локальные z-index.

### Какие ошибки и edge cases связаны с z-index?

Огромные z-index — запах неправильной модели слоёв. Назовите симптом в проде и как поймать тестом или метрикой.

### Какие альтернативы z-index и когда они лучше?

Сравните минимум два подхода по сложности, perf и риску. Сначала структура context, потом локальные z-index.
