/**
 * Rewrites FACTS triples in fill-all-content.mjs to gold quality.
 * Run: node scripts/_rewrite-facts.mjs
 */
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
import vm from 'node:vm'

const root = path.dirname(fileURLToPath(import.meta.url))
const target = path.join(root, 'fill-all-content.mjs')
const src = fs.readFileSync(target, 'utf8')
const start = src.indexOf('const FACTS = {')
const end = src.indexOf('\nfunction factFor')
if (start < 0 || end < 0) throw new Error('FACTS block not found')

const block = src.slice(start, end)
const oldFacts = vm.runInNewContext(block + '\nFACTS')

/** Gold callbacks — do not change */
const CALLBACKS = [
  'Callback — это функция, которую передают другой функции, чтобы та вызвала её позже.',
  'Чтобы не зашивать действие внутрь функции, а передавать его снаружи.',
  'Глубокая вложенность и Node-style (err, data) легко превращаются в проглоченные ошибки; обычно поверх ставят Promise/async.',
]

/**
 * Curated [definition, why, pitfall] for every key.
 * Keys must match runtime FACTS keys.
 */
const R = {
  callbacks: CALLBACKS,

  html5: [
    'HTML5 — это современный стандарт разметки страницы с семантическими тегами, формами и встроенными multimedia API.',
    'Чтобы структура документа была понятна браузеру, поисковикам и вспомогательным технологиям без лишнего «div-супа».',
    'Замена landmark-тегов голыми div ломает навигацию скринридеров и ухудшает SEO.',
  ],
  cascade: [
    'Cascade — это алгоритм CSS, который решает, какое из конфликтующих правил победит: origin, специфичность и порядок в коде.',
    'Чтобы перекрытия стилей были предсказуемыми, а не зависели от случайного порядка подключений.',
    'Злоупотребление !important и «войны специфичности» делают стили почти неподдерживаемыми.',
  ],
  'box model': [
    'Box model — это модель размера элемента: content, padding, border и снаружи margin.',
    'Чтобы ширины и отступы в вёрстке считались одинаково во всех компонентах.',
    'Путаница content-box и border-box ломает сетку: заданная width начинает включать или не включать padding/border.',
  ],
  responsive: [
    'Responsive — это подход, при котором раскладка и типографика подстраиваются под ширину viewport через media queries и гибкие единицы.',
    'Чтобы один интерфейс нормально работал на телефоне, планшете и десктопе без отдельных «мобильных сайтов».',
    'Фиксированные px-ширины и отсутствие meta viewport делают страницу неюзабельной на мобильных.',
  ],
  flex: [
    'Flexbox — это одномерная модель раскладки, которая распределяет элементы вдоль главной оси и выравнивает по поперечной.',
    'Чтобы быстро собирать ряды и колонки компонентов без ручного расчёта отступов.',
    'Путают justify-content и align-items, а неожиданный flex-shrink сжимает элементы без явного запрета.',
  ],
  grid: [
    'CSS Grid — это двумерная модель раскладки по строкам и колонкам с tracks, areas и gap.',
    'Чтобы задавать каркас страницы и сложные сетки без вложенных костылей из float/flex.',
    'Неверное понимание fr и auto-placement ставит элементы не в те ячейки, которые ожидали.',
  ],
  alignment: [
    'Alignment — это выравнивание элементов по осям во flex/grid и выравнивание содержимого внутри ячеек.',
    'Чтобы управлять позицией блоков относительно свободного пространства, а не подпирать margin’ами.',
    'Значение stretch по умолчанию растягивает детей и ломает ожидания фиксированной высоты/ширины.',
  ],
  layout: [
    'Layout (reflow) — это этап, на котором браузер считает геометрию элементов в потоке документа.',
    'Чтобы понимать стоимость изменений размеров/позиций и не устраивать лишние пересчёты.',
    'Чередование чтения геометрии и записи стилей в цикле вызывает layout thrashing и jank.',
  ],
  landmarks: [
    'Landmarks — это регионы страницы (header, nav, main, aside, footer), по которым вспомогательные технологии строят карту документа.',
    'Чтобы пользователь скринридера мог прыгать к основному содержимому и навигации без обхода всей вёрстки.',
    'Несколько элементов main или полное отсутствие main путает навигацию по регионам.',
  ],
  headings: [
    'Иерархия заголовков h1–h6 — это оглавление документа для людей и вспомогательных технологий.',
    'Чтобы смысл разделов читался из структуры тегов, а не только из визуального размера шрифта.',
    'Пропуск уровней и несколько несвязанных h1 разрушают структуру оглавления.',
  ],
  aria: [
    'ARIA — это набор ролей, состояний и свойств, которые дополняют HTML, когда нативной семантики не хватает.',
    'Чтобы кастомные виджеты оставались понятными скринридерам так же, как нативные контролы.',
    'ARIA на неклавиатурном «див-кнопке» хуже нативного button; первое правило — не использовать ARIA, если есть HTML.',
  ],
  labels: [
    'label — это подпись, связанная с контролом через for/id или обёртку, которая даёт полю доступное имя.',
    'Чтобы клик по тексту фокусировал поле и скринридер объявлял назначение ввода.',
    'Placeholder вместо label пропадает после ввода и плохо заменяет настоящую подпись.',
  ],
  validation: [
    'Валидация — это проверка входных данных по правилам (HTML constraint API и/или схема на клиенте и обязательно на сервере).',
    'Чтобы отсекать некорректный ввод рано для UX и никогда не доверять только клиенту для безопасности.',
    'Только клиентская проверка обходится напрямую к API; сервер обязан валидировать снова.',
  ],
  autocomplete: [
    'autocomplete — это атрибут, который подсказывает браузеру и менеджеру паролей семантику поля (email, current-password и т.д.).',
    'Чтобы автозаполнение логина, адреса и платежных данных работало предсказуемо.',
    'Неверные или пустые токены ломают менеджеры паролей и ухудшают security UX.',
  ],
  keyboard: [
    'Клавиатурный доступ — это возможность полностью управлять интерфейсом через Tab, Enter/Space, Esc и стрелки по паттерну виджета.',
    'Чтобы интерактив был доступен без мыши и соответствовал ожиданиям a11y-паттернов.',
    'Сломанный tab order и отсутствие focus trap/restore в модалке делают диалог неуправляемым с клавиатуры.',
  ],
  focus: [
    'Focus — это текущий элемент, который получает клавиатурный ввод; :focus-visible помогает показать индикатор только когда нужно.',
    'Чтобы пользователь всегда видел, где находится фокус, и мог продолжить работу после закрытия оверлеев.',
    'outline: none без видимой замены делает клавиатурную навигацию слепой.',
  ],
  'screen readers': [
    'Screen reader — это программа, которая озвучивает или выводит шрифтом Брайля доступное имя, роль и состояние элементов.',
    'Чтобы интерфейс был пригоден для пользователей, которые не опираются на визуальную вёрстку.',
    'Иконки-кнопки без accessible name «молчат» и не объявляют действие.',
  ],
  stacking: [
    'Stacking context — это изолированный слой, внутри которого сравнивается порядок перекрытия потомков.',
    'Чтобы контролировать, какие элементы рисуются поверх других, без хаотичных глобальных z-index.',
    'Новый context (opacity < 1, transform, filter, position + z-index) часто объясняет, почему «z-index не работает».',
  ],
  'z-index': [
    'z-index — это числовой порядок отрисовки внутри одного stacking context, а не глобальный приоритет по всей странице.',
    'Чтобы локально управлять перекрытием соседних элементов в одном контексте.',
    'Гигантские z-index — запах неправильной модели слоёв: элемент сравнивается не с тем «соседом».',
  ],
  'containing block': [
    'Containing block — это прямоугольник предка, относительно которого считаются проценты размеров и смещения absolute/fixed.',
    'Чтобы правильно позиционировать оверлеи, sticky и процентную геометрию.',
    'transform на предке делает его containing block для fixed — элемент «прилипает» не к viewport.',
  ],
  transform: [
    'transform — это свойство, которое визуально смещает, масштабирует или поворачивает элемент, часто на композитном слое без пересчёта layout.',
    'Чтобы анимировать движение дешевле, чем через top/left, и не форсировать reflow на каждый кадр.',
    'transform создаёт новый containing block и stacking context — ломает ожидания про fixed и z-index.',
  ],
  opacity: [
    'opacity — это степень непрозрачности элемента целиком; значение меньше 1 создаёт новый stacking context.',
    'Чтобы плавно показывать/скрывать UI без перестройки геометрии.',
    'opacity: 0 не убирает элемент из hit-testing и фокуса так, как это делает visibility/inert-паттерн.',
  ],
  'reduced motion': [
    'prefers-reduced-motion — это медиафича, которой система сообщает, что пользователю нужно меньше анимации.',
    'Чтобы уважать vestibular и accessibility-настройки и не навязывать декоративный motion.',
    'Полный игнор запроса вреден; обычно оставляют короткие смысловые переходы и отключают декоративные.',
  ],
}

// Continue building R in this file — imported by splice below after full map is written
export { R, CALLBACKS, oldFacts, target, src, start, end }
